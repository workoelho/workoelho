// This component will be rendered when the user navigates away from the other pages inside @modal segment.
// Otherwise the modal would always be mounted, even when the user navigates back to the parent route.
export default function Page() {
  return null;
}
